doctype html
html
  head
    title user list
    script(src='script.js')
    style 
  body
    h1 This is my project n stuff, it needs more deep frying.
    h4 this is the page where you read the table.
    a(href='/createuser') create user
    table
      tr
        td name
        td id
        td email
        td age
        td delete user
        td add user
      each user in data
        tr
          td= user.name
          td= user.id
          td= user.email
          td= user.age
          td
            button(onclick='deleteUser(this, \'' + user.id + '\')') delete user
          td
            button(onclick='editUser(this, \'' + user.id + '\')') edit user
